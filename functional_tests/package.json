{
  "name": "bedrock-functional-tests",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "compose:up": "docker-compose -f ../docker-compose.yml up -d --build",
    "compose:down": "docker-compose -f ../docker-compose.yml down",
    "compose:wait": "node scripts/wait-for-stack.js",
    "test:compose": "sh -c \"npm run compose:up && npm run compose:wait && npm test; status=$?; npm run compose:down; exit $status\"",
    "test:coverage": "sh -c \"node scripts/wait-for-stack.js && PW_COVERAGE=true playwright test && nyc report --reporter=html --reporter=lcov --temp-dir coverage --report-dir coverage-report\""
  },
  "devDependencies": {
    "@playwright/test": "^1.58.1",
    "nyc": "^17.1.0"
  }
}